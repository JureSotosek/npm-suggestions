type Query {
  suggest(dependencies: [String]!): [Package]!
  search(query: String!): [Package]!
}

type Package {
  name: String!
  version: String
  description: String
  owner: String
  humanDownloadsLast30Days: String
}
