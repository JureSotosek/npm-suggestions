type Query {
  search(dependencies: [Dependency]!): [SearchPayload]!
}

type SearchPayload {
  package: Package!
  score: Int!
}

type Package {
  name: String!
}

input Dependency {
  name: String!
}
