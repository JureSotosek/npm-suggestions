type Query {
  search(dependencies: [Dependency!]!): [PackagesPayload!]!
}

# Types ----------------------------------------------------------------------

type Dependency {
  name: String!
  version: String!
  type: DependencyType!
}

enum DependencyType {
  DEPENDENCY
  DEVDEPENDENCY
  PEERDEPENDENCY
}

type Package {
  name: String!
  version: String!
}

type PackagesPayload {
  score: Number!
  type: DependencyType!
  package: Package!
}

# Inputs ---------------------------------------------------------------------

input Dependency {
  name: String!
  version: String!
  type: DependencyType!
}

# Schema ---------------------------------------------------------------------

schema {
  query: Query
}
